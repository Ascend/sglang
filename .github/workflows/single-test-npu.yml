name: Single Test (NPU)
# test round 1: 1) test_npu_cuda_graph_bs.py - 校验 CUDA Graph decode/prefill 各阶段 bs 参数
#               2) test_npu_msprobe_dump_config.py - 校验 --msprobe-dump-config 使能 msProbe 并在 eager 模式下 dump

on:
  pull_request:
    branches:
      - testcases
      - testcases-260813
    paths:
      - ".github/workflows/single-test-npu.yml"

concurrency:
  group: single-test-npu-${{ github.ref }}
  cancel-in-progress: true

jobs:
  single-node-poc:
    name: single-node-poc
    runs-on: linux-aarch64-a3-2
    container:
      image: swr.cn-southwest-2.myhuaweicloud.com/base_image/dockerhub/lmsysorg/sglang:cann9.1.0-a3-B070
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check npu info
        run: |
          npu-smi info

      - name: Install dependencies
        timeout-minutes: 30
        shell: bash
        run: |
          # msprobe (PyPI 包名 mindstudio-probe) 是 --msprobe-dump-config 的运行依赖,
          # 由 test_npu_msprobe_dump_config.py 使用; 其余依赖 (aiohttp/orjson/requests 等)
          # 镜像内已自带。
          if python -c "import msprobe" 2>/dev/null; then
            echo "msprobe: available, skip install"
          else
            # 共享 pip 源列表: 依次尝试, 任一成功即完成安装
            PIP_INDEXES=(
              "https://pypi.org/simple/"
              "https://mirrors.tuna.tsinghua.edu.cn/pypi/web/simple"
              "http://cache-service.nginx-pypi-cache.svc.cluster.local/pypi/simple"
            )
            PIP_TRUSTED_HOST=cache-service.nginx-pypi-cache.svc.cluster.local
            ok=0
            for index in "${PIP_INDEXES[@]}"; do
              echo "mindstudio-probe: try via ${index}"
              if pip install mindstudio-probe --pre --retries 3 --timeout 60 \
                  -i "${index}" --trusted-host "${PIP_TRUSTED_HOST}"; then
                ok=1; break
              fi
            done
            [ "${ok}" = "1" ] || { echo "ERROR: failed to install mindstudio-probe"; exit 1; }
          fi
          python -c "import msprobe; print('msprobe import ok:', msprobe.__file__)"

      - name: Run test
        timeout-minutes: 120
        env:
          SGLANG_USE_MODELSCOPE: true
          HF_ENDPOINT: https://hf-mirror.com
          SGLANG_IS_IN_CI: true
        shell: bash
        run: |
          sglang_source_path=$(pwd)
          echo "Source code path: ${sglang_source_path}"
          ln -sf ${sglang_source_path} /root/sglang

          # Test cases - 使用时替换为实际的测试用例路径
          test_cases=(
            "test/registered/npu/basic_function/optimization_debug_options/test_npu_cuda_graph_bs.py"
            "test/registered/npu/basic_function/msprobe/test_npu_msprobe_dump_config.py"
          )

          for test_case in "${test_cases[@]}"; do
            if [ ! -f "${sglang_source_path}/${test_case}" ]; then
              echo "The testcase does not exit: ${sglang_source_path}/${test_case}"
              exit 1
            fi
          done

          # copy required file from our daily cache
          cp ~/.cache/modelscope/hub/datasets/otavia/ShareGPT_Vicuna_unfiltered/ShareGPT_V3_unfiltered_cleaned_split.json /tmp
          cp ~/.cache/modelscope/hub/datasets/tmp/test.jsonl /tmp

          echo "scaling_governor performance num: \
            $(cat /sys/devices/system/cpu/cpu*/cpufreq/scaling_governor | grep performance | wc -l)"
          echo "swappiness: $(cat /proc/sys/vm/swappiness)"
          echo "numa_balancing: $(cat /proc/sys/kernel/numa_balancing)"
          echo "sched_migration_cost_ns: $(cat /proc/sys/kernel/sched_migration_cost_ns)"

          export SGLANG_TEST_MAX_RETRY=0
          export SGLANG_SET_CPU_AFFINITY=1
          echo "SGLANG_SET_CPU_AFFINITY: $SGLANG_SET_CPU_AFFINITY"

          echo "Use sglang from image"
          sglang_pkg_path=/sgl-workspace/sglang/python
          ascend_test_util_path=${sglang_pkg_path}/sglang/test/ascend
          mkdir -p ${ascend_test_util_path}
          mv ${ascend_test_util_path} ${ascend_test_util_path}_bak
          cp -r ${sglang_source_path}/python/sglang/test/ascend ${ascend_test_util_path}

          # Set environment of cann
          source /usr/local/Ascend/cann/set_env.sh || true
          source /usr/local/Ascend/nnal/atb/set_env.sh || true

          current_date=$(date +%Y%m%d)
          log_path="/root/.cache/tests/logs/log/${current_date}/${HOSTNAME}"
          rm -rf ${log_path}
          mkdir -p ${log_path}
          echo "Log path: ${log_path}"

          for test_case in "${test_cases[@]}"; do
            tc_name=$(basename ${test_case} .py)
            echo "Running test case ${test_case}"
            python -u ${sglang_source_path}/${test_case}
            echo "Finished test case ${test_case}"
          done

          plog_path="/root/ascend/log/debug/plog"
          if [ -d "$plog_path" ];then
            echo "Plog files found. Begin to backup them."
            target_plog_path="/root/.cache/tests/logs/plog/${HOSTNAME}"
            echo "Save path: ${target_plog_path}"
            rm -rf ${target_plog_path}
            mkdir -p ${target_plog_path}
            cp ${plog_path}/* ${target_plog_path}
          fi
